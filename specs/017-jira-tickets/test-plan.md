# Test plan — delta-017
<!-- derivado dos cenários DADO/QUANDO/ENTÃO da spec e das verificações do tasks.md -->
- [x] CT1 — projeto com `motores: jira` no doc-profile: fase tasks conclui → `tickets.md` nasce com épico `[delta-NNN]`, 1 linha por task, deps preservadas, sem rede · cobre: R1 · tipo: auto · verificação: python3 skills/spec-feature/scripts/tickets.py --selftest
- [x] CT2 — projeto sem `motores: jira`: projeção se omite com 1 linha de aviso e código 0 (degradação RNF2 do TRUTH) · cobre: R1 · tipo: auto · verificação: selftest do tickets.py (caso doc-profile sem jira)
- [x] CT3 — chave devolvida gravada no `tickets.md` (coluna Externo) e ticket com Externo preenchido é pulado no exportar (idempotência) · cobre: R1 · tipo: auto · verificação: selftest do tickets.py (caso Externo preenchido)
- [x] CT4 — `debito.py exportar --projeto`: emite `.sh` de creates unitários com corpo multi-linha byte-idêntico nos `corpo-*.md`; `create-bulk` ausente da saída · cobre: R2 · tipo: auto · verificação: python3 skills/handoff/scripts/projecao.py --selftest + debito.py --selftest
- [x] CT5 — sem chave de projeto informada: dialeto Jira não é emitido; DEBT.md segue valendo com 1 linha de aviso (degradação RNF2 do TRUTH) · cobre: R2 · tipo: auto · verificação: selftest do debito.py (caso sem --projeto)
- [x] CT6 — `tickets.py diff` cobre as 5 divergências (issue fechada×task aberta; task concluída×issue aberta; issue órfã; ticket sem issue; épico aberto×delta arquivada) em tabela formato R27, sem escrever no repo · cobre: R3 · tipo: auto · verificação: selftest do tickets.py (casos do diff)
- [x] CT7 — C11: `jira` ligado sem `projeto` → ALTO; `jira: {projeto: SBX}` → sem achado; ausência → sem achado · cobre: R1 · tipo: auto · verificação: python3 skills/spec-feature/scripts/check_cycle.py --selftest
- [x] CT8 — execução real no SBX: corpo multi-linha íntegro no ticket criado; épico + filhas + link de bloqueio visíveis; diff acusa divergência forjada (issue fechada à mão) e nada é aplicado sem aprovação · cobre: R1, R2, R3 · tipo: manual · verificação: (1) executar tickets-acli.sh da fixture; (2) `acli jira workitem view <chave> --json` confere description; (3) fechar 1 issue no Jira; (4) `acli jira workitem search --jql ... --json > jira.json`; (5) `tickets.py diff --externo jira.json` mostra a tabela; (6) confirmar que nenhum arquivo do repo mudou
