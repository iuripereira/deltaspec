# Tasks — delta-031
<!-- ordenado por dependência; cada task executável sem contexto extra. Arestas de bloqueio: `(dep: Tn)` logo após o ID; task sem `dep:` é livre. -->
- [x] T1 — CLI + caso vermelho: `--rodape`/`--marca-dagua` no argparse (default desligado) e caso novo no `selftest` com as duas flags, assertando rodapé em toda página do pdf, marca no pdf, rodapé no XML do footer docx e `textpath` no header docx · arquivos: skills/doc-entregavel/scripts/exporta_entregavel.py · cobre: R1 · verificação: TDD — o caso falha antes da implementação dos formatos
- [x] T2 (dep: T1) — pdf: blocos CSS `.rodape-conf` e `.marca-dagua` (fixed, rotacionada, atrás do texto) injetados condicionalmente em `monta()`; sem flag, HTML idêntico ao vigente · arquivos: skills/doc-entregavel/scripts/exporta_entregavel.py · cobre: R1 · verificação: metade pdf do caso do selftest verde; caso vigente sem flags inalterado
- [x] T3 (dep: T1) — docx: rodapé via `section.footer` e marca d'água VML canônica do Word no cabeçalho de toda seção · arquivos: skills/doc-entregavel/scripts/exporta_entregavel.py · cobre: R1 · verificação: metade docx do caso do selftest verde; mutação removendo o VML quebra o selftest
- [x] T4 (dep: T2, T3) — doc no mesmo change: passo 4 da SKILL.md cita as flags para `juridico-nda`/`requisitos-cliente` Versão B; `juridico.md` troca a instrução manual pela flag (a regra de conteúdo continua dele) · arquivos: skills/doc-entregavel/SKILL.md, skills/doc-entregavel/references/juridico.md · cobre: R1 · verificação: grep das flags nos dois arquivos; `validate_integrity.py .` PASS
- [x] T5 (dep: T4) — registrar: entrada no CHANGELOG sob `[Não lançado]` e linha no HANDOFF; o quito do DT-011 no `DEBT.md` acontece no archive, junto da consolidação · arquivos: CHANGELOG.md, HANDOFF.md · cobre: infra · verificação: `check_cycle.py specs/031-rodape-marca-dagua` sem CRÍTICO
