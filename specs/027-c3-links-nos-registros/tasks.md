# Tasks — delta-027
<!-- ordenado por dependência; cada task executável sem contexto extra. Arestas de bloqueio: `(dep: Tn)` logo após o ID; task sem `dep:` é livre. T1 e T2 tocam o mesmo laço do C3, então T2 depende de T1; T3 depende das duas por documentar o comportamento final. -->
- [ ] T1 — conjuntos próprios para C2 e C3 no `validate_integrity.py`: `scan_valores` (C2, inalterado) e `scan_links` (C3), com `exclude_links_globs` lido do manifesto e a constante `EXCLUDE_LINKS_PADRAO` como default; fixtures no `selftest()` para arquivo excluído do C2 com link morto (deve FAIL), histórico imutável sem a chave (deve PASS) e com a chave em `[]` (deve FAIL) · arquivos: skills/guarding-doc-integrity/scripts/validate_integrity.py · cobre: R1 · verificação: TDD — cada fixture falha antes do passo que a resolve; `--selftest` verde no fim
- [ ] T2 (dep: T1) — o C3 ignora o atalho `../../issues/N` e `../../pull/N` no mesmo `startswith` que já corta `http://`, `mailto:`, `#` e `/`, com fixture de regressão · arquivos: skills/guarding-doc-integrity/scripts/validate_integrity.py · cobre: R1 · verificação: TDD — fixture com `[#88](../../issues/88)` acusa antes e passa depois; `validate_integrity.py .` sai PASS com o `DEBT.md` no escopo
- [ ] T3 (dep: T1, T2) — propagar o contrato: `exclude_links_globs` no `templates/deps.toml` da skill (com o comentário do porquê) e no `deps.toml` deste repo, docstring do módulo refletindo os dois conjuntos · arquivos: skills/guarding-doc-integrity/references/templates/deps.toml, deps.toml, skills/guarding-doc-integrity/scripts/validate_integrity.py · cobre: R1 · verificação: `validate_integrity.py .` → PASS com mais links verificados que os 105 da baseline
- [ ] T4 (dep: T3) — registrar: DT-027 quitado no `DEBT.md`, entrada no CHANGELOG sob `[Não lançado]` e linha no HANDOFF · arquivos: DEBT.md, CHANGELOG.md, HANDOFF.md · cobre: infra · verificação: `check_cycle.py specs/027-c3-links-nos-registros` sem CRÍTICO
