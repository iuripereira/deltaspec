# Tasks — delta-029
<!-- ordenado por dependência; cada task executável sem contexto extra. Arestas de bloqueio: `(dep: Tn)` logo após o ID; task sem `dep:` é livre. T1 e T2 tocam a mesma função, então T2 depende de T1; T3 depende das duas por medir o resultado combinado. -->
- [x] T1 — recorte por seção lançada: constante `CORTE_LANCADO`, função pura `linhas_vivas(texto)` devolvendo `(nº, linha)` até a primeira `## [X.Y.Z]`, e o laço do C3 passando a consumi-la; fixture com link morto na seção `[Não lançado]` e outro em `[1.0.0]` exigindo exatamente 1 achado · arquivos: skills/guarding-doc-integrity/scripts/validate_integrity.py · cobre: R1 · verificação: TDD — a fixture falha antes; mutação trocando o padrão por `^## \[` (que engoliria `[Não lançado]`) quebra o selftest
- [x] T2 (dep: T1) — sintaxe citada não é referência: `CODE_SPAN` removido da linha antes do `findall` e estado de bloco cercado por ```; fixture com o mesmo link morto dentro de crase (deve passar) e fora dela na linha seguinte (deve falhar), as duas metades no mesmo caso · arquivos: skills/guarding-doc-integrity/scripts/validate_integrity.py · cobre: R1 · verificação: TDD — fixture vermelha antes; mutação removendo o corte de crase quebra o selftest
- [x] T3 (dep: T1, T2) — medir o aceite no repo que originou o débito e conferir a não-regressão: `validate_integrity.py` contra o `imex-travelplanner` sai de 96 para 3 links mortos, e contra este repo continua PASS · arquivos: — (medição, sem edição) · cobre: R1 · verificação: os dois números conferidos por execução, com os 3 remanescentes nomeados no analyze
- [x] T4 (dep: T3) — registrar: progresso do DT-028 no `DEBT.md` (quita no archive, precedente das deltas 026–028), entrada no CHANGELOG sob `[Não lançado]` e linha no HANDOFF · arquivos: DEBT.md, CHANGELOG.md, HANDOFF.md · cobre: infra · verificação: `check_cycle.py specs/029-c3-precisao` sem CRÍTICO
