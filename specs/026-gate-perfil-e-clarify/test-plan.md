# Test plan — delta-026
<!-- derivado dos cenários DADO/QUANDO/ENTÃO da spec e das verificações do tasks.md — não inventa cenário novo (R3, delta-015) -->
<!-- teste manual roteirizado conta como cobertura; todo Rn/RNFn da spec precisa de ≥1 caso (C8) -->
- [x] CT1 — perfil de núcleo íntegro não acusa nada · cobre: R1 · tipo: auto · verificação: fixture do `selftest_c11` com `version`, `decisao{data,justificativa}`, `publico{interno,cliente}` e as 4 categorias de núcleo → lista de achados vazia
- [x] CT2 — chave ou categoria de núcleo ausente, vazia ou do tipo errado acusa ALTO nomeando a chave · cobre: R1 · tipo: auto · verificação: fixtures do `selftest_c11` removendo `version`, removendo `casos-de-uso`, declarando `version:` sem valor, removendo `decisao.justificativa`, com `publico.interno` como string e com `obrigatorio` como string → 1 ALTO cada, com o nome da chave na mensagem (as quatro últimas entraram no review: mutação que apagava cada regra sobrevivia ao selftest)
- [x] CT3 — cauda opcional não é erro · cobre: R1 · tipo: auto · verificação: fixture com `explicativos` presente → zero achado; fixture sem `explicativos`/`prototipo`/`apresentacao` → zero achado (é o caso de 3 a 6 dos 7 perfis reais)
- [x] CT4 — perfil sem obrigatório e sem justificativa acusa ALTO · cobre: R1 · tipo: auto · verificação: fixture com todos `obrigatorio: false` e `justificativa: ""` → 1 ALTO citando a regra do `cycle.md`
- [x] CT5 — `motores.graphify` ligado sem backend acusa ALTO · cobre: R1 · tipo: auto · verificação: fixture com `graphify: true` e `graphify_backend: ''` → 1 ALTO citando R44/ADR-0022; com backend preenchido → zero achado
- [x] CT6 — YAML inválido reporta, não estoura · cobre: R1 · tipo: auto · verificação: fixture com sintaxe quebrada → 1 ALTO com a primeira linha do erro do parser, e o processo termina normalmente (sem traceback)
- [x] CT7 — perfil ausente é BAIXO, e nada do C11 é CRÍTICO · cobre: R1 · tipo: auto · verificação: diretório vazio → 1 BAIXO; em todas as fixtures do `selftest_c11`, `"CRÍTICO" not in severidades`
- [x] CT8 — C12 exige a trilha no perfil completo e dispensa no enxuto · cobre: R1 · tipo: auto · verificação: fixtures do `selftest_c12` — `entrevistado` → 0 ALTO · `auto-avaliado` → 0 ALTO · sem linha e perfil completo → 1 ALTO · sem linha e perfil enxuto → 0 ALTO
- [x] CT9 — sintaxe do clarify citada em prosa ou fora do cabeçalho não conta como trilha · cobre: R1 · tipo: auto · verificação: fixture com `Clarify: entrevistado` no meio de uma frase → 1 ALTO; fixture com a linha íntegra **no corpo do documento**, depois do primeiro `##` → 1 ALTO (regressão das lições de 2026-07-28 e 2026-08-01; a segunda entrou no review, que pegou o check lendo o documento inteiro em vez do cabeçalho)
- [x] CT10 — o contrato normativo do clarify declara canal humano e viés · cobre: R2 · tipo: manual · verificação: (1) `cycle.md` tem a trilha do clarify na tabela de auditoria e o critério de saída cita o canal humano; (2) `adapters.md` diz que exploração do repositório não conta como resposta do usuário; (3) `adapters.md` registra que quem redige a spec é quem pontua o relatório e manda escolher o grau mais ambíguo; (4) o template `delta-spec.md` tem a linha `Clarify:`
- [x] CT11 — o gate roda na própria delta sem acusar o C12 · cobre: R2 · tipo: auto · verificação: `python3 skills/spec-feature/scripts/check_cycle.py specs/026-gate-perfil-e-clarify` não lista achado de `spec.md` sobre trilha do clarify — a spec desta delta carrega a linha que institui
- [x] CT12 — a exceção do PyYAML está nos quatro espelhos e nenhum promete zero-dep · cobre: RNF1 · tipo: auto · verificação: o grep negativo das frases de zero-dep (incluídas `stdlib pura` e `zero dependência externa`, que só o `SECURITY.md` usava) sobre `CLAUDE.md README.md README.en.md SECURITY.md` retorna vazio, e `grep -c "ADR-0023"` retorna ≥1 em cada — o quarto espelho apareceu no review
- [x] CT13 — o CI instala a dependência antes de qualquer step que a importe · cobre: RNF1 · tipo: manual · verificação: no `.github/workflows/ci.yml` o passo `pip install pyyaml` vem logo após o `checkout`, antes do `Validar YAML` (que já fazia `import yaml`) e dos selftests
- [ ] CT15 — o job `ci` passa no PR · cobre: RNF1 · tipo: manual · verificação: checks verdes no PR da delta — só verificável depois de aberto
- [x] CT14 — os 7 perfis reais não produzem falso ALTO · cobre: R1 · tipo: manual · verificação: rodar o C11 contra os perfis de `~/code` — os desvios acusados são só os já medidos em 2026-08-02 (nenhum tem `graphify_backend`; um está em `version: 2`, que o check não reprova por não fixar o valor); nenhum perfil íntegro acusa ALTO
