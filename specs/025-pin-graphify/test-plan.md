# Test plan — delta-025
<!-- derivado dos cenários DADO/QUANDO/ENTÃO da spec e das verificações do tasks.md — não inventa cenário novo (R3, delta-015) -->
<!-- teste manual roteirizado conta como cobertura; todo Rn/RNFn da spec precisa de ≥1 caso (C8) -->
- [ ] CT1 — o adapter declara o que o `--code-only` entrega e o que ele cega · cobre: R1 · tipo: manual · verificação: (1) a seção graphify tem o bullet de modos citando AST/tree-sitter, "zero LLM" e a lista de formatos pulados (.md, PDF, DOCX, XLSX, imagem); (2) o bullet afirma que `AMBIGUOUS` não aparece nesse modo; (3) `grep -n "code-only" skills/spec-feature/references/adapters.md` mostra a preferência dentro do bullet de modos, não mais no bullet de habilitação
- [ ] CT2 — pin verificado por execução real na tabela de política · cobre: R1 · tipo: manual · verificação: a linha do graphify declara versão testada 0.9.32, faixa "pin na testada", verificação datada 2026-08-02 citando a execução no imex-travelplanner, e o risco de bus factor = 1 permanece
- [ ] CT3 — proibição de instalador cobre o alvo por plataforma · cobre: R1 · tipo: manual · verificação: o bullet de habilitação nomeia `graphify install` **e** `graphify claude install` como proibidos, com o motivo (hook PreToolUse + CLAUDE.md) e o link para a ADR-0014
- [ ] CT4 — backends sem fronteira nova são a primeira escolha · cobre: R1 · tipo: manual · verificação: o bullet de backend nomeia `claude-cli` e `ollama` antes de qualquer API paga, com o porquê de cada um (assinatura sem API key; localhost sem transmissão)
- [ ] CT5 — campo do perfil existe e o YAML segue válido · cobre: R1 · tipo: auto · verificação: `python3 -c "import yaml; d=yaml.safe_load(open('skills/projeto-init/references/templates/doc-profile.yaml')); assert d['motores'] == {'graphify': False, 'graphify_backend': ''}, d['motores']; print('ok')"`
- [ ] CT6 — campo vazio com docs pedidos faz parar e perguntar · cobre: R1 · tipo: manual · verificação: (1) o bullet de backend do adapters manda "pare e pergunte" com campo vazio; (2) o comentário do campo no template diz o mesmo; (3) o adapter registra que em `--code-only` o campo é dispensável
- [ ] CT7 — projeto com `publico.cliente: true` decide o backend explicitamente · cobre: R1 · tipo: manual · verificação: o adapter afirma que nesse caso a escolha é do usuário e fica registrada, nunca default da IA
- [ ] CT8 — arquivo citado inexistente não vira claim confirmado · cobre: R1 · tipo: manual · verificação: (1) o bullet do arquivo fantasma manda conferir existência antes de o claim entrar em artefato; (2) diz que inexistente marca `inferido`, nunca `confirmado`; (3) explica a causa (spec descreve código ainda não escrito), para não ser lido como defeito do motor
- [ ] CT9 — nenhuma regra duplicada fora do dono canônico · cobre: R1 · tipo: auto · verificação: `grep -c "claude-cli\|ollama\|0.9.32" skills/descoberta/SKILL.md` retorna 0 e `python3 skills/guarding-doc-integrity/scripts/validate_integrity.py .` retorna `RESULTADO: PASS`
- [ ] CT10 — o graphify segue opcional e desligado por default · cobre: R1 · tipo: auto · verificação: `grep -n "graphify: false" skills/projeto-init/references/templates/doc-profile.yaml` casa, e nenhum arquivo do repo liga o motor por default
