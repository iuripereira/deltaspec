# Test plan — delta-025
<!-- derivado dos cenários DADO/QUANDO/ENTÃO da spec e das verificações do tasks.md — não inventa cenário novo (R3, delta-015) -->
<!-- teste manual roteirizado conta como cobertura; todo Rn/RNFn da spec precisa de ≥1 caso (C8) -->
<!-- o adapters.md quebra em ~80 colunas: normalize whitespace antes de concluir que uma âncora falta (lição 2026-08-01 do DEBT) -->
- [x] CT1 — o adapter declara o que o `--code-only` entrega e o que ele cega, sem preferência normativa · cobre: R1 · tipo: manual · verificação: (1) o bullet de modos cita AST/tree-sitter, "zero LLM" e os formatos pulados (`.md`, PDF, DOCX, XLSX, imagem); (2) afirma que `AMBIGUOUS` nunca aparece nesse modo; (3) a única menção a "preferência" + `--code-only` no arquivo é a que registra que ela **caiu** (ponteiro para a ADR-0022) — nenhuma linha ainda prefere o modo
- [x] CT2 — pin verificado por execução real na tabela de política · cobre: R1 · tipo: manual · verificação: a linha do graphify declara versão testada 0.9.32, faixa "pin na testada", verificação datada 2026-08-02 citando a execução no imex-travelplanner, e o risco de bus factor = 1 permanece
- [x] CT3 — proibição de instalador cobre o alvo por plataforma · cobre: R1 · tipo: manual · verificação: o bullet de habilitação nomeia `graphify install` **e** `graphify claude install` como proibidos, com o motivo (hook PreToolUse + CLAUDE.md) e link markdown para a ADR-0014
- [x] CT4 — backend do modo docs: recomendação, registro e parada · cobre: R1 · tipo: manual · verificação: (1) o bullet nomeia `claude-cli` e `ollama` antes de qualquer API paga; (2) manda registrar em `motores.graphify_backend`; (3) campo vazio com docs pedidos → "pare e pergunte", nunca default; (4) declara o campo dispensável em `--code-only`; (5) o comentário do campo no template diz o mesmo
- [x] CT5 — campo do perfil existe e o YAML segue válido · cobre: R1 · tipo: auto · verificação: `python3 -c "import yaml; d=yaml.safe_load(open('skills/projeto-init/references/templates/doc-profile.yaml')); assert d['motores'] == {'graphify': False, 'graphify_backend': ''}, d['motores']; print('ok')"` — a igualdade de dict também assere `graphify: false`, o default desligado
- [x] CT6 — arquivo citado inexistente não vira claim confirmado · cobre: R1 · tipo: manual · verificação: o bullet manda conferir existência antes de o claim entrar em artefato e marca inexistente como `inferido`, nunca `confirmado`
- [x] CT7 — os três cenários carregados sem mudança seguem sustentados pelo adapter · cobre: R1 · tipo: manual · verificação: (1) mapeamento `EXTRACTED`/`INFERRED`/`AMBIGUOUS` → `confirmado`/`inferido`/`lacuna` com fonte `arquivo:linha`; (2) o eixo Spec do review consta entre as fases que consultam o grafo; (3) o fallback grep/Explore com ≤1 linha de aviso continua declarado
- [x] CT8 — trilha de supersede da ADR-0014 completa · cobre: R1 · tipo: auto · verificação: `grep -c "Superseded by.*ADR-0022" docs/adrs/ADR-0014-*.md` = 1 · `grep -c "Supersedes.*ADR-0014" docs/adrs/ADR-0022-*.md` = 1 · a linha da ADR-0014 em `docs/adrs/README.md` marca "Superseded by 0022"
- [x] CT9 — nenhuma regra duplicada fora do dono canônico · cobre: R1 · tipo: auto · verificação: `grep -c "claude-cli\|ollama\|0.9.32" skills/descoberta/SKILL.md` retorna 0, a skill não repete a regra do modo (só aponta), e `python3 skills/guarding-doc-integrity/scripts/validate_integrity.py .` retorna `RESULTADO: PASS`
