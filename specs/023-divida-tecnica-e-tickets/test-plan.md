# Test plan — delta-023
<!-- derivado dos cenários DADO/QUANDO/ENTÃO da spec e das verificações do tasks.md — não inventa cenário novo (R3, delta-015) -->
<!-- teste manual roteirizado conta como cobertura; todo Rn/RNFn da spec precisa de ≥1 caso (C8) -->
- [ ] CT1 — Tabela migrada tem as 4 colunas novas, item de natureza `guarda` fica com `—` em Título/Local/Fila e nenhum `stale` está escrito no arquivo · cobre: R1 · tipo: auto · verificação: `python3 skills/handoff/scripts/debito.py fila . && ! grep -q "| stale" DEBT.md`
- [ ] CT2 — Os cinco estados são aceitos e um estado inventado é recusado · cobre: R1 · tipo: auto · verificação: fixture do `--selftest` com um item por estado + um `status: pendente` recusado com mensagem nomeando o DT
- [ ] CT3 — Score confere com a fórmula e a ordenação põe override antes de trilha, e trilha antes do maior score · cobre: R2 · tipo: auto · verificação: fixture do `--selftest` compara a ordem devolvida com a esperada
- [ ] CT4 — Item pontuável sem `Local`, com link morto, sem `Título` ou com `Fila` malformada é recusado nomeando o DT e o campo · cobre: R2 · tipo: auto · verificação: quatro fixtures no `--selftest`, cada uma assertando exit ≠ 0 e o `DT-NNN` na mensagem
- [ ] CT5 — Nenhum valor de score é gravado em arquivo · cobre: R2 · tipo: auto · verificação: `python3 skills/handoff/scripts/debito.py fila . && git diff --quiet DEBT.md`
- [ ] CT6 — Probabilidade é derivada do churn em repo com git e a derivação se omite com aviso sem git · cobre: R2 · tipo: auto · verificação: fixture do `--selftest` com repositório git real (padrão `selftest_c7`) + caso sem git
- [ ] CT7 — `stale` é derivado do git e some quando a linha é tocada · cobre: R2 · tipo: auto · verificação: fixture com dois commits datados, assertando a marca antes e a ausência depois
- [ ] CT8 — Célula que menciona a sintaxe em prosa não engana o parser · cobre: R2 · tipo: auto · verificação: fixture de regressão com a palavra "aberto" dentro de uma célula de status de item quitado (lição 2026-07-28)
- [ ] CT9 — `exportar` emite JSON canônico válido, dialeto bulk do Jira e linhas de criação do GitHub, sem tocar a rede · cobre: R3 · tipo: auto · verificação: `debito.py exportar` + `json.load` nos dois JSON + ausência de import de rede no módulo
- [ ] CT10 — Issue criado carrega a etiqueta com o `DT-NNN` e o título prefixado pelo ID; a chave volta para a coluna `Externo` · cobre: R3 · tipo: manual · verificação: (1) rodar `exportar`; (2) criar um issue pelo comando emitido; (3) `gh issue view <n> --json title,labels`; (4) conferir a chave gravada na linha do DEBT.md
- [ ] CT11 — Reexecutar a ida não duplica ticket · cobre: R3 · tipo: manual · verificação: (1) rodar a ida de novo com a coluna `Externo` já preenchida; (2) conferir que os itens já projetados são pulados por chave, não por título
- [ ] CT12 — `diff` acusa item sem ticket, ticket fechado com item aberto e ticket sem item · cobre: R3 · tipo: auto · verificação: fixture do `--selftest` com um estado externo sintético cobrindo os três casos
- [ ] CT13 — Divergência vira proposta, nunca escrita automática · cobre: R3 · tipo: auto · verificação: `debito.py diff` + `git diff --quiet DEBT.md` (o subcomando não altera o arquivo)
- [ ] CT14 — Sem `gh`/sem autenticação, o fluxo degrada com aviso e o DEBT.md segue válido · cobre: R3 · tipo: manual · verificação: (1) rodar `fila` e `exportar` com `PATH` sem `gh`; (2) confirmar exit 0 e no máximo 1 linha de aviso
