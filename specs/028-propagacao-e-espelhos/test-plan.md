# Test plan — delta-028
<!-- derivado dos cenários DADO/QUANDO/ENTÃO da spec e das verificações do tasks.md — não inventa cenário novo (R3, delta-015) -->
<!-- teste manual roteirizado conta como cobertura; todo Rn/RNFn da spec precisa de ≥1 caso (C8) -->
- [x] CT1 — perfil sem `version` é válido · cobre: R1 · tipo: auto · verificação: fixture do `selftest_c11` sem o campo → **zero** achado (hoje espera 1 ALTO; a inversão é o vermelho do TDD)
- [x] CT2 — perfil **com** `version` continua válido · cobre: R1 · tipo: auto · verificação: fixture com o campo presente → zero achado; chave fora do núcleo nunca é erro, então nenhum dos 7 perfis reais passa a falhar
- [x] CT3 — o resto do núcleo continua exigido · cobre: R1 · tipo: auto · verificação: as fixtures vigentes de `decisao`, `publico` e categoria ausente seguem acusando 1 ALTO cada — a delta afrouxa um campo, não o check
- [x] CT4 — o campo saiu dos dois lugares que o distribuem · cobre: R1 · tipo: auto · verificação: `grep -c "^version:" skills/projeto-init/references/templates/doc-profile.yaml doc-profile.yaml` retorna 0 nos dois, e o comentário do topo do template registra a retirada
- [x] CT5 — a ADR está no dono e nos três espelhos, e em mais nenhum varrido · cobre: RNF1 · tipo: auto · verificação: `validate_integrity.py .` → `PASS`, com a linha `[C1] ... OK (dono + 3 espelho(s))` do padrão novo e nenhum `[C2]`
- [x] CT6 — o C1 pega espelho dessincronizado · cobre: RNF1 · tipo: manual · verificação: mutação — apagar `ADR-0023` do `README.en.md` numa cópia → `validate_integrity.py` sai 1 nomeando o arquivo; restaurar
- [x] CT7 — o TRUTH.md entra como espelho antes do archive · cobre: RNF1 · tipo: manual · verificação: com o `[[owner]]` novo e o TRUTH ainda citando a ADR, o pré-commit passa; retirar o TRUTH dos espelhos faz o C2 acusá-lo — é o furo de ordem registrado na spec
- [x] CT8 — a metade negativa continua verificada e declarada · cobre: RNF1 · tipo: manual · verificação: o `ci.yml` mantém o grep das frases de zero-dep com comentário nomeando a lacuna (`validate_integrity` sem check de padrão proibido), e o loop `grep -q ADR-0023` saiu, porque o C1 o cobre
- [x] CT9 — o init relata perfil atrás do template sem escrever sozinho · cobre: R2 · tipo: manual · verificação: a `SKILL.md` do `projeto-init` manda comparar categorias, **relatar** a diferença e escrever só com aprovação explícita, e diz que `decisao`, `publico` e `obrigatorio` já declarados nunca são tocados
- [x] CT10 — a recusa do usuário não vira reescrita automática · cobre: R2 · tipo: manual · verificação: a mesma prosa registra que o relato é informativo e que o RNF3 (2ª execução não altera arquivo versionado) continua valendo
