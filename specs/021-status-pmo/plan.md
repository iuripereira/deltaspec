<!-- resumo deltaspec · ≤15 linhas · única parte do plano lida pelo analyze e pelo humano -->
**Objetivo:** extrair o padrão de site de status PMO validado no caso IMEX como skill `status-pmo` (processo + templates; gerador vive no repo cliente). **Cobre:** R1, R2 (delta-021) **Decisões duráveis → ADRs:** ADR-0019 (skill + renúncias: SaaS/BI, gerador genérico no plugin, lib de gráfico) **Riscos assumidos:** repetição do gerador por repo (padrão documentado, não código compartilhado).

Execução: SKILL.md (frontmatter `name`+`description` EN com triggers; corpo PT-BR: overview→invariantes→6 gates→erros comuns) + `references/templates/` (styles-tokens.css neutralizado do caso IMEX, theme.js, cronograma/ata templates, dados-schema.md) + citação nos 2 manifestos (`plugin.json` e `marketplace.json` — CI exige) + CHANGELOG/HANDOFF. TDD não se aplica (conteúdo declarativo, zero script novo — justificativa desta linha); validação = CI (frontmatter, JSON, commits) + check_cycle.
