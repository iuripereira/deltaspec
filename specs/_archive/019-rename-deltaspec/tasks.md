# Tasks — delta-019
<!-- ordenado por dependência; cada task executável sem contexto extra.
     Arestas de bloqueio (delta-016): `(dep: Tn[, Tm])` logo após o ID — task sem
     `dep:` é livre. Duas tasks sem caminho entre si no grafo são paralelizáveis
     (execução por worktree: cycle.md); o C9 valida existência e aciclicidade. -->
- [x] T1 — renomear o repositório no GitHub para `deltaspec`, atualizar `git remote set-url` e a description do repo · arquivos: nenhum (estado remoto) · cobre: R1 · verificação: `git remote -v` aponta `iuripereira/deltaspec` e `gh repo view iuripereira/deltaspec` responde
- [x] T2 (dep: T1) — trocar identidade e URLs nos manifestos · arquivos: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` · cobre: R1 · verificação: `python3 -m json.tool .claude-plugin/plugin.json` e `python3 -m json.tool .claude-plugin/marketplace.json` parseiam, e `grep -rc sdd-iuri .claude-plugin/` retorna 0
- [x] T3 (dep: T1) — trocar a chave de config do validador para `deltaspec.validator` no template do hook e na SKILL.md que instrui a configurá-la · arquivos: `skills/guarding-doc-integrity/templates/pre-commit`, `skills/guarding-doc-integrity/SKILL.md` · cobre: R5 · verificação: `bash -n skills/guarding-doc-integrity/templates/pre-commit` e `grep -c 'sdd-iuri' skills/guarding-doc-integrity/` retorna 0
- [x] T4 (dep: T1) — trocar `/sdd-iuri:` → `/deltaspec:` no `description:` do frontmatter das 7 SKILL.md que citam o namespace como gatilho · arquivos: `skills/{descoberta,doc-entregavel,handoff,projeto-infra,projeto-init,spec-feature,spec-review}/SKILL.md` · cobre: R2, R3, R4 · verificação: `head -4 skills/*/SKILL.md | grep -c 'sdd-iuri'` retorna 0
- [x] T5 (dep: T2, T3, T4) — trocar o namespace e o nome nos arquivos vivos restantes, sem tocar `specs/_archive/`, CHANGELOG lançado e ADRs 0001–0015 · arquivos: `README.md`, `CLAUDE.md`, `specs/TRUTH.md`, `HANDOFF.md`, `skills/**/references/**`, `skills/**/templates/**`, `skills/**/scripts/*.py`, `scripts/instala-motores.sh`, `.githooks/pre-commit`, `.github/workflows/ci.yml` · cobre: R1, R2, R3, R4, R5, RNF1 · verificação: `grep -rl 'sdd-iuri' --exclude-dir=_archive --exclude-dir=.git .` lista apenas `CHANGELOG.md`, `docs/adrs/ADR-00{01,04,08,11,16}*.md` e a seção de migração do README
- [x] T6 (dep: T5) — escrever a seção "Migrando de sdd-iuri" no README com os passos de reinstalação e reconfiguração · arquivos: `README.md` · cobre: R5 · verificação: a seção existe e cita `/plugin marketplace remove sdd-iuri`, `add iuripereira/deltaspec`, `install deltaspec@deltaspec` e `git config deltaspec.validator`
- [x] T7 (dep: T5) — registrar a mudança no CHANGELOG sob `[Não lançado] / Mudado` com rodapé `BREAKING CHANGE` · arquivos: `CHANGELOG.md` · cobre: R1 · verificação: a entrada cita o namespace antigo e o novo e o `[Não lançado]` continua no topo
- [x] T8 (dep: T6, T7) — rodar os gates e o grep de resíduo · arquivos: nenhum · cobre: R1, R5 · verificação: `python3 skills/spec-feature/scripts/check_cycle.py --selftest`, `python3 skills/guarding-doc-integrity/scripts/validate_integrity.py --selftest` e `python3 skills/spec-feature/scripts/check_cycle.py specs/019-rename-deltaspec` saem 0
- [x] T9 (dep: T8) — abrir o PR da delta com `ci` e `commits` verdes · arquivos: nenhum · cobre: R1 · verificação: `gh pr checks` verde
