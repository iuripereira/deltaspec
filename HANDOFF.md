# HANDOFF.md — diário de bordo
> Andamento contínuo do trabalho: o que está em curso **agora**, o que acabou de ser feito, os problemas do momento e os próximos passos imediatos. Atualize com frequência dentro da própria sessão. **Janela rolante:** entrada antiga sai — histórico permanente é [CHANGELOG](CHANGELOG.md) + git; débito/pendência/lição é [DEBT.md](DEBT.md); decisão com renúncia é [docs/adrs/](docs/adrs/); o que vige é [specs/TRUTH.md](specs/TRUTH.md). Em conflito de merge, mantenha a **união das verdades** — nunca sobrescreva o progresso de outra sessão.
**Atualizado em:** 2026-08-04
## Agora
- **delta-031 (rodapé/marca d'água no export, quita DT-011) em curso** na branch `feat/031-rodape-marca-dagua`: analyze LIBERADO, implement TDD verde (selftest com o caso confidencial nos dois formatos; mutações pegam). A **delta-030 fechou hoje**: `v1.9.0`, DT-029 quitado. Decisões do usuário em 2026-08-04: ilustração datada e perfil enxuto para as duas deltas.
- **Nenhuma outra delta em curso.** Framework na **`v1.8.1`**; deltas 001–016 e 018–029 arquivadas; TRUTH R1–R52 + RNF1–RNF6. Fila de dívida em 11 itens (`python3 skills/handoff/scripts/debito.py fila .`), nada com gatilho disparado.
- **Replicação nos consumidores CONCLUÍDA (2026-08-03).** `claude plugin marketplace update deltaspec` + `claude plugin update deltaspec@deltaspec` levaram o plugin de `f1555620` (**v1.5.0**) para `b3ea8fa` (**v1.8.1**). O install é **user-scoped**: um update move os 11 consumidores de uma vez — não existe escalonar por repo. Aceite medido depois: integridade documental `PASS` nos 4 repos com `deps.toml`, e o hook de pré-commit do `imex-travelplanner` (que resolve o validador pelo cache do plugin) voltou a **sair 0**.
- **Preparo feito antes do update, com os hooks ainda no validador antigo:** trilha do clarify declarada nas 4 deltas abertas dos consumidores — `imex` (`d749ae0`), `radar-financeiro` (`e1d47eb`), `imex-travelplanner` (`fa1fd89`) —, com a data da própria delta e nota em comentário de que a declaração é **retroativa**. Nenhum registro de resposta do usuário existia nas quatro, então saiu `auto-avaliado`, que é o que a regra manda.

## Feito recentemente
- 2026-08-03 — **DT-029 projetado como [#117](https://github.com/iuripereira/deltaspec/issues/117)** (`exportar` + `gh issue create`, chave gravada no campo `Ticket`, `diff` reconferido em 0 achados). O passo anterior deste diário listava 4 DTs a projetar, mas DT-025, DT-026 e DT-028 foram **quitados pelas deltas 028/029 na mesma sessão** — e quitado não se projeta: a gramática do registro troca `Ticket` por `Encerrado`, e tanto o `exportar` quanto o `diff` só apontam item aberto. Só o DT-029 era projetável.
- 2026-08-03 — **Quatro deltas fechadas e a replicação executada.** [delta-026](specs/_archive/026-gate-perfil-e-clarify/) (C11 do doc-profile + C12 da trilha do clarify, `v1.7.0`, quita DT-013/DT-023) · [delta-027](specs/_archive/027-c3-links-nos-registros/) (C3 com conjunto próprio, `v1.7.1`, quita DT-027) · [delta-028](specs/_archive/028-propagacao-e-espelhos/) (`version` fora do perfil + política sob o `deps.toml`, `v1.8.0`, quita DT-025/DT-026) · [delta-029](specs/_archive/029-c3-precisao/) (C3 para na seção lançada e ignora sintaxe citada, `v1.8.1`, quita DT-028). Detalhe de cada uma no CHANGELOG e no apêndice de review do respectivo `analyze.md`.
- 2026-08-03 — **A delta-029 nasceu de defeito provado por um consumidor**, não por dogfood: este repo tem 3 meses de CHANGELOG e passava no próprio gate; o `imex-travelplanner` tem um ano e o C3 acusava 96 links mortos, travando os commits de lá pelo hook. 89 estavam em seções lançadas do changelog — histórico imutável pelo Keep a Changelog. É a primeira vez que a evidência externa que o DT-004 cobrava por dois meses aponta um furo antes do dono notar.
- 2026-08-03 — **Achado do review que vale carregar:** medir "quantos falsos positivos sumiram" não é medir a mudança. Na delta-029 eu reportei 96 → 3 como ganho; o review mostrou que **240 links saíram da varredura** no mesmo repo e **3 eram referências vivas** (docstring em bloco cercado, pseudocódigo, crase de tabela). A troca vale, mas é troca — e o número que eu tinha escolhido só media um dos lados.
- 2026-08-02 — **delta-026 (gate do doc-profile e canal humano no clarify) implementada**: C11 e C12 no `check_cycle.py`, MUDA R12/R8, ADICIONA RNF de política de dependência, [ADR-0023](docs/adrs/ADR-0023-pyyaml-como-dependencia-admitida.md). Quita **DT-013** e **DT-023**. O gatilho do DT-013 ("formato estabilizado por delta real em projeto externo") **já tinha disparado havia semanas** e ninguém tinha varrido — a mesma cegueira da lição de 2026-07-31: 7 perfis reais em `~/code`, 3 deles com delta. A varredura também deu o desenho do check: núcleo em 7/7, cauda (`explicativos` 4/7, `prototipo`/`apresentacao` 1/7) nunca propagada, então o C11 exige o núcleo e **tolera a cauda** — rodado contra os 7, zero falso ALTO. Dois achados do próprio TDD: o gate acusou ALTO legítimo no specify (o R3 declarava `MUDA` de um RNF que não existe no TRUTH — virou `ADICIONA`), e o selftest pegou lacuna de desenho (delta `bugfix` tem clarify sob demanda e precisava dispensar o C12). `PyYAML` entra como **única dependência externa admitida**: renunciamos ao parser próprio porque o modo de falha dele é silencioso.
- 2026-08-02 — **delta-025 (pin do graphify) arquivada** ([#103](https://github.com/iuripereira/deltaspec/pull/103) + PR de archive): MUDA R44 consolidado com 7 cenários, ADR-0022, `v1.6.0`. **Primeira adoção real do 4º motor**, o que a ADR-0014 tinha contratado só pela doc: `graphify 0.9.32` no `imex-travelplanner`, backend `claude-cli` (assinatura, custo zero), 235 documentos → 1.053 nós e 2.752 arestas em ~90 min (o `claude-cli` é forçado a concorrência 1). O contrato de proveniência bateu linha a linha, mas o uso derrubou uma premissa: **`--code-only` cega todo arquivo não-código** e nunca gera `AMBIGUOUS` — a preferência normativa da ADR-0014 caiu, com supersede parcial registrado. Dois achados que só a execução dá: o repo-alvo é 97% documentação (5.235 linhas de código contra 210 markdowns), então o grafo entregue é **de documentação, não de código**; e 16 dos 27 arquivos de código citados **não existem** — as specs descrevem implementação futura. A estimativa de tokens errou por 4,7× (576k projetados × 2,7M reais: o cap de 20k/arquivo não conta o system prompt por chunk). O review em 2 eixos pegou um **CRÍTICO** que o gate não alcança: a reversão da ADR-0014 estava sem trilha de supersede.
- 2026-08-01 — **delta-024 (`DEBT.md` legível) arquivada**: feedback do Iuri logo após a 023 — "as colunas estão confusas", "não ficou claro como a regra é aplicada", "PRs e issues deveriam estar linkados". A tabela de 11 colunas (linha mais longa: 1.549 caracteres) virou **bloco por item** no padrão dos ADRs; PR, issue, delta e artefato viraram links relativos; o cabeçalho ganhou a legenda dos cinco estados. Dois achados durante a conversão: o `stale` mudou de semântica para melhor (mede mudança de **estado**, não de prosa, porque o ID só vive no cabeçalho) e o conversor produzia link errado — `delta-001` do repo imex apontando para a delta-001 daqui —, corrigido restringindo o link de delta ao campo `Origem`.
- 2026-08-01 — **delta-023 (dívida técnica com score e projeção para tickets) arquivada** (#86 artefatos + #97 implementação + PR de archive): MUDA R18 e R51/R52 novos no TRUTH, `v1.4.0`, DT-022 roteado (propagar ao template distribuído depois do dogfood). `DEBT.md` com `Título`/`Local`/`Fila`/`Externo` e os estados `aceito`/`vigente`/`descartado`; `skills/handoff/scripts/debito.py` (fila, exportar, diff — stdlib, sem rede, com selftest de git real); ADR-0020 (modelo) e ADR-0021 (projeção, **supersede a ADR-0007** na parte das Issues, pela cláusula que a própria 0007 escreveu). **Ida e volta validadas de ponta a ponta:** os 8 débitos abertos viraram os issues #87–#94, as chaves voltaram para a coluna `Externo`, e o `diff` detectou corretamente o fechamento forçado do #93 antes de voltar a zero. Três achados de integração que só apareceram na execução real, já corrigidos: link relativo não resolve dentro de issue (virou caminho literal), `gh issue create` recusa etiqueta inexistente (o roteiro passou a criá-las) e `--label dt` não casa `dt:DT-NNN` (a coleta usa `--search` com vírgula, que é OR).
- 2026-07-31 — **DT-004 quitado: a evidência externa existia havia uma semana e ninguém tinha visto.** A varredura de prontidão para publicação achou a delta-001 do `~/code/imex` arquivada em **2026-07-24** com ciclo completo — `analyze.md` C1–C7 limpos e LIBERADO (a 1ª rodada acusou ALTO legítimo no `cobre:` do T3, ou seja o gate pegou erro real fora do framework), archive e consolidação no `TRUTH.md` de lá (R1–R5 + RNF1–RNF2), release `v0.1.0` — e o ciclo continua vivo no repo (delta-002 `aplicada`, delta-003 `proposta`). O repo não tem remote, a mesma cegueira da lição do rename: lição nova registrada no DEBT. **Não fecha** DT-013 (imex sem `doc-profile.yaml`) nem DT-017 (sem medição). Confirmado de passagem que o **reporte privado de vulnerabilidade já está habilitado** (o passo pendente do SECURITY.md saiu daqui).
- 2026-07-31 — **delta-022 (`status-pmo`: épicos, tarefas e dependências) arquivada** (#83 + PR de archive): R50 novo, MUDA R48/R49, `v1.3.0`. Reflete no dono as lições da 1ª revisão do PO no caso imex — gate de épicos (`docs/epicos/<dir>.md`), página por épico com dependências e registros, grafo em SVG inline, tema claro como padrão nos assets e escopo rastreado explícito. Perfil enxuto, test-plan dispensado.
- 2026-07-30 — **delta-020 (apresentação: Figma → diagram-design + design-sync) arquivada** ([#77](https://github.com/iuripereira/deltaspec/pull/77) + PR de archive): a decisão do usuário antecipou o gatilho do DT-014 — MUDA R45/R46 no TRUTH, ADR-0018 supersede a ADR-0015, DT-014 quitado, `v1.1.0`. Perfil enxuto (2º dogfood), review fundido APROVADO COM AJUSTES (eixo Spec 9/9; achados de higiene tratados — o commit do SECURITY.md que estava na branch virou o [#76](https://github.com/iuripereira/deltaspec/pull/76)). Na mesma sessão: ADR-0017 (Claude Code only, DT-018 quitado, [#75](https://github.com/iuripereira/deltaspec/pull/75)) e DT-016–018 registrados do benchmark SDD ([#74](https://github.com/iuripereira/deltaspec/pull/74)). Fechando a rodada, DT-015 quitado no [#79](https://github.com/iuripereira/deltaspec/pull/79) (README.en.md + CONTRIBUTING + código de conduta).
- 2026-07-30 — **SECURITY.md criado no padrão da comunidade** (pesquisa real em claude-code, OpenSpec, Electron e Express via `gh api`): reporte privado via Security Advisories, SLA (3 dias úteis / 30 dias), suporte = última tag, modelo de ameaça de plugin local com tabela escopo/fora de escopo. Detalhe no CHANGELOG (`[Não lançado]`). **Pendência operacional:** o reporte privado de vulnerabilidade do repo está desabilitado e o classificador bloqueou o `gh api -X PUT` — habilitar manualmente (próximos passos).
## Problemas atuais
- **Nenhum bloqueio.** O gate deste repo sai `LIBERADO` limpo e o `validate_integrity` sai `PASS` (162 links).
- **O ciclo cobra débito próprio dos consumidores, e não é regressão do update:** no `~/code/imex` a delta-002 está `aplicada` fora do `_archive/` (ALTO) e falta `doc-profile.yaml` (BAIXO); no `imex-travelplanner` a delta-001 não tem `tasks.md` nem `test-plan.md` e os artefatos passaram do limiar de split. São dívidas daqueles repos, anteriores à v1.8.1 — pertencem ao `DEBT.md` de cada um, não a este.
- **Quatro reviews seguidos acharam o que o gate não alcança** (026, 027, 028, 029), e em três o furo estava **no ponto que a delta existia para reforçar**. É o [DT-023](DEBT.md) se pagando: nenhum passaria por check determinístico. O review em dois eixos não é cerimônia — é a única rede que pegou esses.

## Próximos passos imediatos
- **[DT-029](DEBT.md) é o candidato natural à delta-030** (`P3·J1·Pr3`): cenário do TRUTH carrega número medido que apodrece — o do atalho do GitHub diz "19 links do DEBT.md" e hoje são 23 —, e o bloco MUDA repete byte a byte por regra, carregando o número defasado adiante. Decidir entre ilustração datada, nota no analyze ou check que recalcula.
- **Débito de ciclo dos consumidores:** o umbrella `~/code/imex` e o `imex-travelplanner` têm débito de ciclo próprio (acima, em Problemas atuais). Se for tratar, é no `DEBT.md` de cada repo — o deltaspec só os mede.
- **Resíduo do rename, medido em 2026-08-03 e menor do que o diário dizia:** sobraram **2 arquivos** citando `sdd-iuri` em instrução viva — `~/code/imex/specs/003-site-status/tasks.md` e `~/code/radar-financeiro/HANDOFF.md`. O `imex-nao-conformidade` não existe mais em `~/code`. O resto das ocorrências é registro de época (CHANGELOG lançado, `_archive/`, ADR `Accepted`), que o R47 manda preservar. Os dois repos são sem remote — fechamento por commit local.
- Fase 4 do plano de upgrade (**delta-017 — número reservado via R5**, adiada por decisão do usuário 2026-07-28): skill `spec-tickets`, `tickets.md` canônico com Jira como projeção. Abrir só quando o Iuri pedir; destrava DT-021 e o gatilho do fork do max (ADR-0012).
- Rodar uma delta num projeto externo **cronometrando tempo e tokens** — é o que falta para o **DT-017**.
